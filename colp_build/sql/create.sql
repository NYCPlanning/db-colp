DROP TABLE IF EXISTS colp CASCADE;
CREATE TABLE colp(
    borough text,
    block text,
    lot text,
    cd text,
    hnum text,
    street text,
    parcel text,
    agency text,
    use_code text,
    use_type text,
    ownership text,
    leased text,
    final_commit text,
    category_code text,
    expanded_cat_code text,
    mappable text,
    bbl text,
    x_coord text,
    y_coord text
);
